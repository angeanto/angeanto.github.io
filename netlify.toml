# netlify.toml
# This file should be at the root of your project
[build]
  # Search is disabled; deployments must not depend on Algolia indexing.
  command = "bundle exec jekyll build"
  publish = "_site"
